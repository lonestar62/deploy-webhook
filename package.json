{
  "name": "deploy-webhook",
  "version": "1.0.0",
  "description": "GitHub webhook receiver for auto-deploy on fleet-vm",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  }
}
